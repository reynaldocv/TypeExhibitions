# Uncomment the following to override menu entry labels in the "History" menu
#
# menuItemNames = {
#	ca_objects = Items
# }

change = {
    table = "ca_occurrences", 
    type = "exhibitions", 
    prevParameter = "tipo_exposição", 
    nextParameter = "fromMACUSP", 
    values = [
        ["Exposição produzida pelo MAC USP"             , 'Exposição produzida pelo MAC USP'],  
        ["Exhibition previous to MAC USP acquisition"   , 'Exhibitions from other institutions'],  
        ["Exhibition in other institution (loan)"       , 'Exhibitions from other institutions'],  
    ],
}

